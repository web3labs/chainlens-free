apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: epirus-net
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/epirus-net: "true"
  podSelector:
    matchLabels:
      io.kompose.network/epirus-net: "true"
